<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>iBellaPhotos - <%= booking.name %></title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.7.2/font/bootstrap-icons.min.css" rel="stylesheet">
    <link rel="icon" href="images/favicon.png" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cedarville+Cursive&family=Oleo+Script&family=Poppins&display=swap" rel="stylesheet">
    <style>
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
        }
        .booking-container {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;

            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            /* background: url('<%= booking.imageUrl %>') no-repeat center center;
            background-size: cover; */
            min-height: 600px; /* Adjust as needed */
            color: #fff; /* Ensure text is readable on dark backgrounds */
        }

        .blur-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('<%= booking.imageUrl %>') ;
            background-size: cover;
            filter: blur(8px);
            z-index: -1;
        }

        .tidycal-widget {
            width: 100%;
            max-width: 600px;
            height: 670px;
            border: 1px solid #ddd;
            border-radius: 8px;
            overflow: hidden;
            background: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .details{
    
            background-color: #fff;
            padding: 25px;
            color: black;
            border: 1px solid lightgray;
            border-bottom:none;
            border-radius: 5px ;
        }
        .header{
            background-image: url('../dist/images/background-2.jpg');
            
        }
        .black{
            color: black;
        }
    </style>
</head>
<body>
  
  

    <div class="booking-container">
        <div class="blur-overlay"></div>
        <div class="header">
            <div class="header-items">
                <div class="logo">
                    <i class="bi bi-camera icon-large black"></i>
                    <a href="/index.html" class="undecorate"><h1 class="header-text undecorate direct-text">iBellaPhotos</h1></a>
                </div>
                <a href="/about.html" class="header-item direct-text">About</a>
                <a href="/booking.html" class="header-item direct-text">Booking</a>
                <a href="#" class="header-item direct-text gallerylink">Gallery</a>
                <a href="/contact.html" class="header-item direct-text">Contact</a>
            </div>
        </div>
        <div class="details">
            <h1 class="layedback-text">Booking: <%= booking.name %></h1>
            <p class="layedback-text">Price: $<%= booking.price %></p>
            <p class="layedback-text"><%= booking.duration %></p>
            <ul class="layedback-text">
                <% booking.details.forEach(detail => { %>
                 <li>&#8226; <%= detail %></li>
                <% }); %>
            </ul>
        </div>
        <div class="tidycal-widget">
            <div class="tidycal-embed" data-path="<%= booking.dataPath %>"></div>
            <script src="https://asset-tidycal.b-cdn.net/js/embed.js" async></script>
        </div>
    </div>

    <script>
        document.querySelectorAll('.gallerylink').forEach(item => {
          item.addEventListener('click', function() {
            const baseUrl = 'https://ibellaphotosserver.up.railway.app'; // Update with your Railway backend URL
            // const baseUrl = 'http://localhost:3000'; // Update with your local backend URL
            window.location.href = `${baseUrl}/galleries`;
          });
        });
    </script>

</body>
</html>
